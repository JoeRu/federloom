package ingest

// Spamtrap adapter — honeypot semantics inside a real system: never-used
// mailboxes, auth against non-existent accounts. Retains the zero-false-positive
// property on a production box (spec §6.1). Stub.
