package ingest

// CrowdSec adapter — consumes CrowdSec LAPI decisions as Events, and (paired with
// the enforce/crowdsec sink) lets SwarmGuard federate threat intel that CrowdSec
// would otherwise only share with its central community network.
// Complements github.com/JoeRu/Mailcow-Crowdsec-Override. Stub.
