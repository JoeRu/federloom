package ingest

// Honeypot adapter — bridges dedicated honeypot tools (e.g. Cowrie, Dionaea,
// OpenCanary, T-Pot). Any connection there is malicious by definition →
// ground-truth anchor (spec §4.1). Stub. See docs/plugins.md for adding tools.
